<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LagosTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/lagos_primary</item>
        <item name="colorPrimaryDark">@color/lagos_dark</item>
        <item name="colorAccent">@color/lagos_dark</item>

    </style>

    <style name="AnambraTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/anambra_primary</item>
        <item name="colorPrimaryDark">@color/anambra_dark</item>
        <item name="colorAccent">@color/anambra_accent</item>
    </style>

    <style name="AbujaTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/abuja_primary</item>
        <item name="colorPrimaryDark">@color/abuja_dark</item>
        <item name="colorAccent">@color/abuja_accent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
